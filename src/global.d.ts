// Ambient module declarations for side-effect asset imports (e.g. `import 'flatpickr/dist/flatpickr.css'`).
// The build (esbuild) handles these at bundle time; this only satisfies the TypeScript checker.
declare module '*.css';
